
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Login</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>

    <div class="headerSection">
        <h1 class="nameHeader">Uploadify</h1>
    </div>

  <div class="loginFormWrapper">
    <% if (user) {%>
      <h1>WELCOME BACK <%= user.username %></h1>
      <a href="/logout">LOG OUT</a>
    <% } else { %>
      <h1 class="loginLabel" >Please log in</h1>
      <form class="loginForm" action="/login" method="POST">
        <label class="loginLabel" for="username">Email:</label>
        <input id="username" name="username" placeholder="username" type="text" />
        <label class="loginLabel" for="password">Password</label>
        <input id="password" name="password" type="password" />
        <button class="loginBn" type="submit">Log In</button>
      </form>
      <p class="msgFormText">New account? <a class="Link" href="/signup">Sign up here</a></p>
    <%}%>
  </div>
</body>
</html>